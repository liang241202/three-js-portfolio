"use client";

// STUB for Task 3 (lets GoldenSlice import it); the real instanced grass + wind shader lands in Task 4.
export default function GrassField(_: { area: number; count: number }) {
  return null;
}
